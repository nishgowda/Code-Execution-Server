#include <stdio.h>
#include <assert.h>
#include "code.student.public07.h"

/* this represents one test of the functions in code.student.public07.c */

/* this test has syntax errors, so it won't compile! */

int main(void) {
  assert(product(2, 16) == 32

  printf("The student code in code.student.public07.c works on its ");
  printf("third test!\n");

  return 0;
}
